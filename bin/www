#!/usr/bin/env node
'use strict';

require('coffee-script/register');
require('../server/main').listen(process.env.PORT || 3000, (err, result) => {
  console.log(err || 'Listening at localhost:3000');
});
